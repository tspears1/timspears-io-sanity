import { defineType } from 'sanity'
import {ImagesIcon} from '@sanity/icons'

export default defineType({
    type: 'object',
    name: 'imageBlock',
    title: 'Image Block',
    icon: ImagesIcon,
    fields: [

    ]
})